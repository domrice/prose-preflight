# Introduction

The convolutional neural network (CNN) is standard, so CNN needs no second expansion.
We instead evaluate the LSTM on every split, and the LSTM again here.
GPU memory is allowlisted, as is the JSON output.
MLP (multilayer perceptron) is defined the other way round, so MLP is fine.

The `HTTP` token in code is masked and never seen.

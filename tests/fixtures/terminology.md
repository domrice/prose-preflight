# Methods

We assembled a data set of 12k samples and applied pre-processing before
training. The neural net was compared against the ground-truth labels.

```python
# a data set in code is not prose
neural_net = build("pre-processing")
```

Our dataset, our preprocessing and our neural network are already correct here,
and `data set` inside inline code must stay untouched.

## Results

Data-set curation drove the gains; the work flow is reproducible.
